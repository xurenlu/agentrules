<!-- SCAFFOLDED FILE — REVIEW AND CUSTOMIZE.
     Created by Alma scripts/generate_project_agents.rb; the generator will never overwrite it. -->

# <%= module_name %> — Agent Instructions

> **Scope:** `<%= scope %>` 及其子目录。
> **Parent:** [<%= parent_label %>](<%= parent_link %>) — 父级规则继续生效；本文件只补充或覆盖本模块规则。

## 模块上下文

- 构建清单：<%= manifests_markdown %>
- 修改前先继承父级规则；不要复制整篇根规则。

## 常用命令

<%= commands_markdown %>

## 模块特有规则

<%= module_rules_markdown %>
